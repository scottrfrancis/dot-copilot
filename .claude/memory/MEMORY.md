# Project Memory Index

## Key Context

This project ports the `~/.claude/` configuration to GitHub Copilot equivalents. The deliverable is the `copilot/` directory, which gets symlinked into target projects' `.github/` directories via `install.sh`.

## Source Reference

- Original Claude Code config: `~/.claude/`
- Concept mapping: `docs/concept-mapping.md`
- Known limitations: `docs/limitations.md`
